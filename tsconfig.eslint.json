{
  // extend your base config so you don't have to redefine your compilerOptions
  "extends": "./tsconfig.json",
  "include": [
    "bin/**/*.ts",
    "src/**/*.ts",
    "test/**/*.ts",
    "typings/**/*.ts",
    "migrations/**/*.ts",
    "seeders/**/*.ts",
  ]
}
